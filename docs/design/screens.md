# Screen Designs — PixushHR Dashboard

Visual reference for all screens: `docs/design/reference/ui_kits/pixushr/index.html`.

Implementation status (v1):
- [x] **Live Run** — task input → Trigger → response + audit (wired to the engine). *Implemented.*
- [ ] Messages — placeholder ("Not implemented yet")
- [ ] Users & roles — placeholder
- [ ] Synthetic data — placeholder
- [ ] Integrations (Catalog / Installed) — placeholder
- [ ] Workflow editor — placeholder
- App shell (top bar + dark left nav + toggles) — implemented in `dashboard/src/shell/`.
