# Component & Design Guidelines — PixushHR Dashboard

> **To be populated from Claude Design (Prompt 3).** This is the document **any future AI reads
> before building a new component.** It must cover: design principles; when to use each component
> (do/don't); token rules (never hardcode); layout/density/spacing; required states; the
> accessibility checklist; **how to add a new component**; and the implementation target
> (React + TS + Tailwind, tokens as CSS vars, shared `dashboard/src/ui/`), including a PR checklist.
